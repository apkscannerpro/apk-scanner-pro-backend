services:
  - type: web
    name: apk-scanner-backend
    runtime: python
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -b 0.0.0.0:$PORT apk_scanner_pro.api_server:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10
